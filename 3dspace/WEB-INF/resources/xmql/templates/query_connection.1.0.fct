Language: xMQL

start transaction;
label ${label} query connection type ${type} vault ${vault} ${wherekw} ${where} ${orderbykw} ${orderby} ${limitkw} ${limit} select ${select,noescape= } dump | tcl;
abort transaction;
