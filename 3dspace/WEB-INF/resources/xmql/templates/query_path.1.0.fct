Language: xMQL

start transaction;
label ${label} query path type ${type} vault ${vault} ${conditionkw} ${physicalids,noescape= } ${wherekw} ${where} ${orderbykw} ${orderby} ${limitkw} ${limit} select ${select,noescape= } dump | tcl;
abort transaction;
