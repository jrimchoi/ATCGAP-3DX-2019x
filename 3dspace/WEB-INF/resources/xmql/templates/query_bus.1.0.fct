Language: xMQL

start transaction;
label ${label} temp query bus ${type} ${name} ${revision} vault ${vault} ${wherekw} ${where} ${orderbykw} ${orderby} ${limitkw} ${limit} select ${select,noescape= } dump | tcl;
abort transaction;
